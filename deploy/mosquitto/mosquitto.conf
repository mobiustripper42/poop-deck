# Poop Deck :: Mosquitto (local dev)
# One plain listener; anonymous access. The farm box tightens this
# (per-producer credentials / ACLs); local dev stays open.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
log_dest stdout
