---
session: 1
dev: eric
slug: main
branch: main
started: 2026-07-14T19:17:00Z
ended: 2026-07-17T02:04:21Z
points: 16
pr_numbers: [7, 8, 9, 12, 16]
status: closed
transcript: /home/estoffer/.claude/projects/-home-estoffer-poop-deck/491c1039-5a1f-45ab-bcf8-b34aea7db8a5.jsonl
---

# Session 1 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 1.1 — docker-compose stack

**Completed:**
- `deploy/docker-compose.yml` — Mosquitto + TimescaleDB + Grafana, pinned images, named volumes, healthcheck-gated Grafana start, `db/migrations/` mounted to Timescale init dir
- `deploy/mosquitto/mosquitto.conf` — anonymous 1883 listener (dev)
- `db/migrations/0000_enable_timescaledb.sql` — enables TimescaleDB, sorts before hypertable migrations
- `db/migrations/0001_irrigation_runs.sql` — reverted in-place extension edit (now relies on 0000)
- `deploy/README.md` — run commands + dev-exposure note
- Verified on bee-grace: clean replay from empty volume — extension, hypertable, and natural-key unique index all present

**Code review:** 3 cleanup findings, all addressed — migration split into 0000 (no in-place edit), LAN/trust-auth exposure + Grafana default creds documented as deferred prod-hardening.
**PR:** [#7](https://github.com/mobiustripper42/poop-deck/pull/7)
**Points:** 3
**Branch:** task/1.1-docker-compose
**Opened at:** 2026-07-15T04:08:41Z

## Task 2: Phase 1.2 — irrigation_runs smoke check

**Completed:**
- `db/smoke/irrigation_runs_smoke.sql` — re-runnable, side-effect-free (BEGIN…ROLLBACK) smoke check: hypertable exists, natural-key index is UNIQUE, duplicate insert is a no-op (no double-count, no overwrite), natural key spans zone, gallons/min query runs
- `db/README.md` — documented 0000 migration + a Smoke checks section with the run command
- Verified on bee-grace: exit 0, duplicate dropped (`INSERT 0 0`), distinct zone lands, both zones in the query output, table clean after rollback

**Code review:** 2 cleanup findings, both fixed — index check now verifies actual uniqueness (`pg_index.indisunique`), and a third insert proves the key spans zone not just source.
**PR:** [#8](https://github.com/mobiustripper42/poop-deck/pull/8)
**Points:** 2
**Branch:** task/1.2-verify-irrigation-schema
**Opened at:** 2026-07-15T04:20:42Z

## Task 3: Phase 1.3 — wire tinkle ingest daemon + pytest

**Completed:**
- `ingest/irrigation_ingest.py` — refactored into `build_row` / `insert_row` / `on_message` seams; added `isinstance(dict)` guard so non-object JSON drops instead of crashing the daemon
- `tests/test_irrigation_ingest.py` — 23 tests (validate-and-drop, unknown-`v`, unparseable + non-object JSON, never-crash-on-DB-error, happy-path insert, live redelivery no-op)
- `pytest.ini`, `ingest/README.md` (run + test commands)
- `.claude/CLAUDE-context.md` — clarified dedup is proven by the live test, not the fake seam
- Verified on bee-grace: 23 passed with stack up; 22 pass + 1 skip without DB

**Code review:** 2 findings, both fixed — **bug:** `build_row` crashed on valid-JSON-non-object payloads (would kill the daemon on one poison publish, DEC-004); guarded + 10 new tests. **cleanup:** doc gap on where dedup is proven.
**PR:** [#9](https://github.com/mobiustripper42/poop-deck/pull/9)
**Points:** 3
**Branch:** task/1.3-ingest-daemon
**Opened at:** 2026-07-16T03:15:29Z

## Task 4: Phase 1.4 — tinkle irrigation dashboard + Grafana provisioning

**Completed:**
- `dashboards/tinkle/irrigation.json` — phone-usable single-column dashboard: faults-in-range stat, gallons/min by zone, run history, faults table (read-only)
- `dashboards/provisioning/datasources/timescaledb.yml` + `dashboards/provisioning/dashboards/poopdeck.yml` — auto-provisioned datasource + dashboard provider
- `deploy/docker-compose.yml` — Grafana mounts provisioning subdirs + `dashboards/tinkle` (wiring deferred from 1.1); mounts specific subdirs to keep Grafana's plugins/alerting dirs intact
- `dashboards/README.md` — layout documented
- Verified on bee-grace: datasource "Database Connection OK", dashboard provisions clean, all 4 panel queries return correct data (sample rows cleaned up). Visual browser render is the one thing not automatable — eyeball at `http://bee-grace:3000`.

**Code review:** 2 polish findings, both fixed — dashboard set read-only to match provider; README hardening reference fixed to point at prod-hardening / issue #10.
**PR:** [#12](https://github.com/mobiustripper42/poop-deck/pull/12)
**Points:** 5
**Branch:** task/1.4-irrigation-dashboard
**Opened at:** 2026-07-16T17:57:00Z

## Task 5: Phase 1.5 — end-to-end proof (synthetic tinkle publisher)

**Completed:**
- `tools/synth_publish.py` — synthetic tinkle publisher (v1 events over MQTT); waits for CONNACK; `--replay` resends saved payloads for a clock-independent idempotency demo
- `tools/README.md` — tool + full end-to-end procedure
- Proven on bee-grace: publish 6 → daemon → Timescale (6 rows, 1 fault) → dashboard; replay → still 6 rows. Sim data (`source='tinkle-sim'`) left in store so the dashboard shows real numbers.

**Code review:** 3 findings, all fixed — bug (publish race before CONNACK), replay fragility (now replays saved payloads), missing type hints.
**PR:** [#16](https://github.com/mobiustripper42/poop-deck/pull/16)
**Points:** 3
**Branch:** task/1.5-e2e-proof (stacked on 1.4; merged main in after #12 landed)
**Opened at:** 2026-07-16T20:45:18Z

**Phase 1 complete** — all 5 tasks shipped (16 pts). PRs #7, #8, #9, #12 merged; #16 open.

**Also filed (backlog, not Phase 1):**
- #10 — Remote access (reach Grafana from anywhere) — `backlog`
- #11 — Control tinkle from Poop Deck — `backlog`, `needs-decision` (reverses DEC-002 one-way boundary; needs an @architect call)
- #13 — Onboard real tinkle (firmware publishes v1 to the broker; work lives in tinkle repo) — `backlog`
- #14 — Run the ingest daemon as an always-on service — `backlog`
- #15 — Proportionate hardening of bee-grace (kitchen-table LAN tier) — `backlog`

**Next Steps:**
- Merge #16 to close Phase 1, then `/retro` for the phase.
- Consider #15 (hardening) + #14 (always-on daemon) before #13 (real tinkle onboarding) so MQTT creds are set once.
- bee-grace is already on Tailscale (100.105.112.4) — most of #10 may be a short hop.

**Context:**
- Claude runs ON bee-grace as estoffer; drive docker via `sg docker -c "..."` (shell not in docker group without it).
- Local dev stack is trust-auth / anonymous MQTT / admin-admin Grafana — deliberate, deferred to #15.
- Grafana reachable on LAN at 192.168.50.201:3000 (hostname "bee-grace" doesn't resolve from phones; use the IP). ufw is active.
- Review agents bring the stack up to test and may tear it down on finish — just `docker compose up -d` to restore.

**Context:**
